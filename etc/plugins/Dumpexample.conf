# Dump config file
#

# Write the message to this dir
#
DUMP_DIR  = /tmp

# Provide a suffix for the file
#
FILE_SUFFIX = dat

# Provide a prefix for the file
#
FILE_PREFIX = msg_

# Use newline characters as segemtn separators. Otherwise the message will
# be dumped as is.
#
PRETTY_PRINT = 1
